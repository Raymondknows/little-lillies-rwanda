This directory contains backed-up marketing / SEO pages moved from `frontend/src/app`.

Reason: onboarding a per-customer frontend where generic SchoolBase marketing pages are removed.

To restore a page, move the directory back to `frontend/src/app/<name>` and commit.
